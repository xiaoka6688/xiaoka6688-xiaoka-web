# 小卡 - AI实战项目集 - 项目状态

## 📅 更新日期
2026-08-13

## 🎯 项目概述
小卡的个人作品集网站，展示 AI 智能体开发和 AIGC 实战应用能力。
技术栈：React 18 + TypeScript + Vite 5 + Tailwind CSS 3 + Three.js / React Three Fiber + GSAP + Framer Motion + React Router 6 + i18next。

## ✅ 已完成功能

### 核心页面
- [x] 首页（Hero、关于、服务、项目、联系）
- [x] 项目详情页
- [x] 致谢页

### 3D 名片卡片（工牌）
- [x] 内容完整显示（修复双面 UV 折叠导致的右半被裁问题）
- [x] 按 HIKROBOT 参考图重设计为极简工牌风
- [x] **配置驱动**：所有卡片内容抽成 `CardConfig`，由 `buildCardSVG(config)` 生成 SVG
- [x] **实时可编辑**：`CardEditor` 面板支持改头像 / 姓名 / 副标题 / 红条文字 / 显示红条 / 主题色 / 背景渐变
- [x] **自动持久化**：配置存入 `localStorage`（键 `xiaoka-card-config-v1`），刷新不丢
- [x] **默认配置更新**：姓名=小卡，副标题=AI智能体项目应用研究，红条=人工智能，头像=`/avatar-default.jpg`

### 自定义管理后台（方案 A 完成）
- [x] **AdminPanel**：设置面板中的管理入口，支持项目/服务/联系信息管理
- [x] **项目管理**：合并显示静态数据+自定义数据，支持复制并编辑静态项目，4个功能特性编辑
- [x] **服务管理**：合并显示静态数据+自定义数据，支持复制并编辑静态服务
- [x] **联系信息管理**：支持自定义名称/角色/描述/联系方式
- [x] **联系板块配置合并**：工牌编辑+联系信息 Tab 切换
- [x] **图片上传**：支持压缩后存储为 base64
- [x] **数据导入导出**：JSON 格式备份和恢复
- [x] **安全设计**：移除删除按钮，用显示/隐藏替代

### 项目图片自动适配
- [x] `useProjectImage` Hook：按 .png -> .jpg -> .jpeg -> .webp -> .svg 顺序自动探测
- [x] 放图片到 `public/projects/{slug}/` 目录下同名即可自动加载
- [x] 统一命名规则：`hero.png` + `feature-N.{jpg/png}`

### 内容数据
- [x] 11 个项目数据（AI Brand Kit、Logo Design、IP Agent、AI Design、News Trend、MuseBox、抖音运营工具箱、破局AI项目圈、小卡AI绘图、Dreampix-AI、小卡海报编辑器）
- [x] 9 个服务数据
- [x] 关于页面内容（卡片式布局，4个亮点）
- [x] 致谢页面内容

### 导航
- [x] 右上角设置按钮（SettingsPanel）固定可用
- [x] 设置面板包含：功能设置、联系板块配置、自定义管理

### 视觉层级优化
- [x] 统一板块标题风格（SectionTitle 组件）
- [x] 添加板块视觉分隔（渐变分隔线）
- [x] 统一响应式断点
- [x] 关于区域卡片式布局

### 联系方式更新
- [x] Instagram -> 公众号"小卡AI"
- [x] Louie的主页 -> 公众号"破局AI项目圈"
- [x] 新增微信图标

## 🔄 进行中 / 最近完成

### 2026-08-13 完成
- [x] **Hero 开场白文案优化**（commit `2341ec9`）
  - eyebrow 小标签改为「AI 实战项目集」（原「你好，我是小卡」与大标题重复）
  - 描述段落改为方案 A：AI深度研习探索者 · 破局AI项目圈/创业资源站主理人 · 专注AI智能体开发与AIGC项目实战应用
  - 去掉与「关于我」亮点卡片重复的年限描述（10年+无人机/5年+新媒体/3年+AI）
- [x] **默认背景改为 iridescence 虹彩**（commit `96322f2`、`6cdf6da`）
  - `BackgroundSwitcher.tsx` `DEFAULT_BG` 从 `grainient` 改为 `iridescence`
  - `CURRENT_VERSION` 从 `'2'` 升至 `'3'`，强制所有访客重置为虹彩背景
- [x] **dark tone 文字对比度修复**（commit `6e84184`）
  - `globals.css` dark tone 补全 `--color-bg/surface/border` 深色值，修复设置面板等 `bg-surface` 元素白底白字看不清
  - dark tone `--color-muted` 提亮（183 190 206 → 215 220 230）
  - dark tone 下 `text-muted` 文字加 `text-shadow` 描边，增强彩色背景上清晰度
  - FPS watchdog 阈值放宽：慢帧 36→66ms（<15fps），触发 60→150 帧（~10s），避免虹彩默认背景被误回退
- [x] **Hero/About 布局对齐优化**（commit `b932eac`）
  - Hero `max-w-5xl`→`max-w-6xl`，与 About/Projects/Contact 左边界对齐（382px），消除滚动时 64px 横跳
  - About 补 `scroll-mt-28`，修复锚点跳转被固定导航遮挡
  - About `md:pt-40`→`md:pt-28`，减小过大的顶部间距；Hero 加 `pb-16` 底部留白
  - 关于我卡片去掉 `hover:-translate-y-1`，避免不可点击元素的交互误导
- [x] **feature 图接入 ImageLightbox 点击查看大图**（commit `8724ded`）
  - `ProjectDetailPage.tsx` feature 图可点击打开 ImageLightbox 放大查看原尺寸（1200px vs 444px 显示）
  - 加「点击查看大图」hover 提示（含放大镜图标）
  - 支持键盘 Enter/Space 触发、ESC/点遮罩关闭、滚动锁定
  - 解决 feature 图在 `md:grid-cols-2` 布局下显示尺寸过小导致内容不清晰的问题

### 2026-08-12 完成
- [x] **移动端黑屏性能优化**（commit `da483bd`）
  - **代码分割**：`vite.config.ts` 中将 React 独立为 `react-vendor` chunk，与 r3f（3MB）分离
  - 初始加载从 3.4MB 降至 504KB（gzip 152KB），手机端不再长时间黑屏
  - **浅色默认背景**：`globals.css` 中 `:root` 默认背景从深色改为浅色 `#f5f7fc`，消除加载瞬间黑屏
  - **内联加载指示器**：`index.html` 中添加旋转动画 + "加载中…" 文字，React 挂载后自动替换
  - **PWA 配色更新**：`site.webmanifest` 中 `theme_color` 和 `background_color` 同步改为 `#f5f7fc`
- [x] **网站标题更新**（commit `f3d0521`）
  - 浏览器标签页标题从 "小卡 - Portfolio" 改为 "小卡 - AI实战项目集"
  - 同步更新 `index.html`、`public/404.html`、`public/site.webmanifest`、`README.md`、`项目状态.md`、`项目结构说明.md`
- [x] **Favicon 全套适配**（commit `e596b78`）
  - 以 `public/logo.png`（1024×1024）为源图，生成 7 个 favicon 文件
  - 覆盖场景：浏览器标签页、Windows 任务栏、iOS 主屏幕、Android 主屏幕/PWA
  - 新增文件：`favicon.ico`、`favicon-16x16.png`、`favicon-32x32.png`、`favicon-48x48.png`、`apple-touch-icon.png`、`android-chrome-192x192.png`、`android-chrome-512x512.png`、`site.webmanifest`
  - `index.html` 添加 7 个 favicon 相关标签，`theme-color` 更新为 `#0a0e1a`
- [x] **背景默认色迁移至 grainient**（commit `9b2b65e`、`97700b3`）
  - `BackgroundSwitcher.tsx` 默认背景从 `aurora` 改为 `grainient`
  - 新增版本迁移机制：通过 `xiaoka.bgVersion` 清除旧 localStorage 偏好，确保新老访客都看到 grainient
  - 背景分类：轻量级（aurora、grainient、colorbends）和重量级（prism、silk、iridescence、beams）
  - 重量级背景仅在桌面设备 4 核以上进入随机池
- [x] **Vercel 部署迁移**
  - 从 GitHub Pages 迁移到 Vercel，利用香港 CDN 节点提升国内访问速度
  - 自定义域名 `xiaoka.pojuai.com` 绑定 Vercel，DNS 指向 `385d5cc6059c1a92.vercel-dns-017.com.`
  - GitHub 仓库自动关联，push 触发 Vercel 自动部署
  - 原 GitHub Pages 域名仍可访问，但 Vercel 为主要部署平台

### 2026-08-01 完成
- [x] **修复本地预览和线上部署图片/字体路径不匹配问题**
  - GitHub Pages 配置了自定义域名 `xiaoka.pojuai.com`，`vite.config.ts` base 统一为 `/`
  - 新增 `publicAsset()` 工具函数，统一为 public 资源拼接 base 前缀
  - `projectImage.ts` / `cardConfig.ts` / `ServicesSection.tsx` / `CardEditor.tsx` / `SettingsPanel.tsx` 全部接入
  - `@font-face` 从 CSS 移至 `main.tsx` 动态注入，支持 base 路径
  - `BrowserRouter` 添加 `basename`，修复线上深链路由
  - `404.html` SPA shim `pathSegmentsToKeep` 改为 `0`（适配自定义域名）
  - `index.html` 移除硬编码字体 preload
  - 修复 `projects.ts` 中数据层图片路径与实际文件不匹配
- [x] **修复 3D 工牌头像不显示问题**
  - 浏览器安全限制：data URL SVG 无法加载外部资源
  - 解决方案：在 `Lanyard.tsx` 中先将 avatar 图片 `fetch` 转为 data URL，再嵌入 SVG
- [x] 项目图片自动适配扩展名（useProjectImage Hook）
- [x] 统一项目图片命名规则（hero + feature-N）
- [x] 清理废弃目录（7个）
- [x] 视觉层级优化（标题统一、分隔线、断点统一）
- [x] 联系方式更新（公众号替换 Instagram 和 Louie 主页）
- [x] 项目替换（News Trend、MuseBox、AI Design 等）
- [x] 工牌默认配置更新

## 📋 待办事项

### 后续计划
1. **方案 B：云端存储** - Firebase/Supabase 实现多设备同步（约 4 天）
2. **方案 C：完整 CMS** - Strapi 等无头 CMS 系统（约 5 天）

### 优化项
3. **3D 模型压缩** - 压缩 `card-BP4TWJmK.glb`（当前 2.4MB），减少慢网络交互延迟
4. **卡片交互体验** - 悬停/拖动动画优化
5. **主题切换** - 支持深色/浅色主题

## 📁 关键文件

### 自定义管理后台
- `src/types/admin.ts` - 数据类型定义
- `src/services/DataService.ts` - 数据访问层（localStorage）
- `src/components/admin/AdminPanel.tsx` - 管理面板主组件
- `src/components/admin/ProjectManager.tsx` - 项目管理（CRUD + 功能特性编辑）
- `src/components/admin/ServiceManager.tsx` - 服务管理（CRUD）
- `src/components/admin/ContactManager.tsx` - 联系信息管理
- `src/components/admin/ContactSectionManager.tsx` - 联系板块配置（工牌+联系信息）
- `src/components/admin/ImageUploader.tsx` - 图片上传（压缩+base64）
- `src/components/admin/DataTransfer.tsx` - 数据导入导出

### 项目图片适配
- `src/utils/projectImage.ts` - useProjectImage Hook（自动适配扩展名）
- `src/utils/publicAsset.ts` - public 资源路径前缀工具（dev/build 自适应）

### 3D 卡片相关
- `src/components/reactbits/Components/Lanyard/Lanyard.tsx` - 3D 卡片组件
- `src/components/reactbits/Components/Lanyard/cardConfig.ts` - 配置接口 + 默认配置 + SVG 生成器
- `src/components/reactbits/Components/Lanyard/CardEditor.tsx` - 实时编辑面板

### 背景切换
- `src/components/BackgroundSwitcher.tsx` - 7种背景（aurora、grainient、colorbends、prism、silk、iridescence、beams），默认 iridescence
- 轻量级背景（aurora、grainient、colorbends）全设备可用；重量级背景（prism、silk、iridescence、beams）仅桌面 4 核以上
- 版本迁移机制：`xiaoka.bgVersion`（当前 v3）不匹配时清除旧 localStorage 偏好，强制重置为默认虹彩
- FPS watchdog：重量级背景帧率 <15fps 持续 ~10s 才回退到轻量级（阈值已放宽，避免默认虹彩被误切）

### 性能优化
- `vite.config.ts` - 代码分割：react-vendor / r3f / gsap / ogl / motion 独立 chunk
- `index.html` - 内联加载指示器（旋转动画 + "加载中…"）
- `src/styles/globals.css` - 浅色默认背景，防止加载瞬间黑屏
- 初始加载：504KB（gzip 152KB），r3f 按需加载

## 🐛 已知问题
1. **MeshLineGeometry NaN 错误** - 绳子组件偶发 NaN 值警告，不影响卡片显示
2. **3D 模型文件较大** - `card-BP4TWJmK.glb`（2.4MB），慢网络下交互可能延迟

## 🚀 部署状态
- 开发服务器：http://localhost:5173（本地预览正常）
- **主要部署**：Vercel（push main 自动触发，香港 CDN 节点）
- 线上地址：https://xiaoka.pojuai.com/（自定义域名绑定 Vercel）
- 备用部署：GitHub Pages（仍可访问，但非主要通道）
- Vercel 项目地址：https://xiaoka-web.vercel.app

---
*最后更新：2026-08-13*
