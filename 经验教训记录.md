# 经验教训记录

## 📋 问题概述

在添加新服务/项目到网站时，多次出现 **Demo组件复用错误** 的问题。

---

## 🚨 问题案例

### 案例1：破局AI创业
- **问题**：添加「破局AI创业」服务时，`demo` 字段设置为 `'poetry'`，导致展开后显示的是「诗云」的Demo内容
- **影响**：用户看到的是诗歌相关的Demo，与知识付费平台完全无关
- **修复**：创建专属的 `PjhtDemo` 组件

### 案例2：小卡AI绘图
- **问题**：添加「小卡AI绘图」服务时，`demo` 字段再次设置为 `'poetry'`，复用了诗云的Demo
- **影响**：用户看到的是诗歌Demo，与AI绘图工具完全无关
- **修复**：创建专属的 `AiDrawDemo` 组件

---

## 🔍 问题根因

1. **偷懒复用**：为了省事，直接复用已有的Demo组件
2. **缺乏检查**：添加新服务后没有实际展开测试
3. **流程缺失**：没有建立标准化的添加流程

---

## ✅ 正确流程

### 添加新服务的标准步骤：

```
1. 创建专属Demo组件
   └── src/components/sections/services/XxxDemo.tsx

2. 注册Demo组件
   ├── services.ts → DemoId 类型添加 'xxx'
   └── ServicesSection.tsx → DEMO_REGISTRY 添加 xxx: XxxDemo

3. 配置服务数据
   └── services.ts → demo 字段设置为 'xxx'（不是复用其他Demo）

4. 测试验证
   └── 展开服务面板，确认显示的是正确的Demo内容
```

### 关键检查点：

- [ ] Demo组件是否为新服务创建？
- [ ] `demo` 字段是否指向正确的Demo ID？
- [ ] 展开后显示的内容是否与服务匹配？
- [ ] 是否避免了复用其他服务的Demo？

---

## 📝 经验总结

| 序号 | 经验教训 | 避免方法 |
|------|---------|---------|
| 1 | 不要复用其他服务的Demo | 每个新服务必须创建专属Demo |
| 2 | 添加后必须测试 | 展开服务面板验证内容 |
| 3 | 遵循标准流程 | 按照上述步骤逐一完成 |
| 4 | 代码审查 | 检查 demo 字段是否正确 |

---

## 🎯 规则

**核心原则：每个服务/项目必须有独立的Demo组件，严禁复用其他服务的Demo。**

---

## 📅 记录时间

- 案例1记录：2026-07-06
- 案例2记录：2026-07-06
- 文档创建：2026-07-06
- 项目图片自动适配经验：2026-08-01

---

## 🆕 新增经验（2026-08-01）

### 案例3：项目图片自动适配扩展名

#### 问题描述
项目文件夹中放置了 `.jpg` 和 `.png` 混合格式的图片，但代码中硬编码了特定扩展名，导致部分图片无法显示，统一显示"截图待补充"占位符。

#### 根因分析

1. **命名不统一**：不同项目使用了不同的命名规则
   - `ai-draw`：`poster-1.jpg` ~ `poster-4.png`
   - `dreampix`：`template-1.jpg` ~ `template-4.jpg`
   - `musebox`：`feature-1.jpg` ~ `feature-4.jpg`

2. **扩展名硬编码**：`projects.ts` 中写死了 `.jpg` 或 `.png`，无法自动适配

3. **探测方案失败**：
   - **HEAD 请求方案**：开发服务器对静态文件 HEAD 请求返回 405，探测失败
   - **直接渲染 + onError 方案**：`<img>` 和探测 hook 同时加载，`<img>` 加载 `.png` 失败立即触发 `onError`，而 hook 还没来得及尝试 `.jpg`

#### 最终解决方案

创建 `useProjectImage` Hook，按 `.png -> .jpg -> .jpeg -> .webp -> .svg` 顺序用 `Image` 对象探测，**完整探测完所有扩展名后再渲染 `<img>`**：

```typescript
// 返回 { loading, src, failed }
// loading 时显示"加载中..."
// failed 时显示占位符
// src 时显示图片
const { loading, src, failed } = useProjectImage(slug, 'hero');
```

#### 关键经验

| 序号 | 经验教训 | 避免方法 |
|------|---------|---------|
| 5 | 不要硬编码图片扩展名 | 使用自动适配 Hook |
| 6 | HEAD 请求在开发服务器不可靠 | 用 `Image` 对象探测 |
| 7 | 探测和渲染不要同时进行 | 探测完成后再渲染 `<img>` |
| 8 | 统一命名规则 | `hero.png` + `feature-N.{jpg/png}` |

#### 标准流程

```
1. 图片放到 public/projects/{slug}/ 目录
   ├── hero.png 或 hero.jpg
   ├── feature-1.png 或 feature-1.jpg
   └── ...

2. 代码中使用 useProjectImage 自动适配
   const { loading, src, failed } = useProjectImage(slug, 'hero');

3. 无需修改代码，放图片进去刷新即可
```

### 案例4：联系方式类型更新

#### 问题描述
联系方式从 Instagram + Louie主页 改为两个微信公众号，需要同时更新图标、渐变色、发光色。

#### 解决方案
- 在 `ContactSection.tsx` 中新增 `WeChatIcon` 组件
- 更新 `getIconByType`、`getGradientByType`、`getGlowByType` 支持 `wechat` 类型
- 更新 `defaultContactConfig` 中的联系方式数据

#### 关键经验
联系方式类型变更需要同步更新 3 个映射函数（图标/渐变色/发光色），否则视觉不一致。

---

## 🆕 新增经验（2026-08-01 续）

### 案例5：Vite base 路径导致本地预览和线上部署图片全部 404

#### 问题描述
项目部署到 GitHub Pages，`vite.config.ts` 中设置 `base: '/xiaoka6688-xiaoka-web/'`（仓库名）。但代码中所有 public 资源（图片、字体、头像）都用 `/` 开头引用（如 `/projects/xxx/hero.png`、`/fonts/JetBrainsMono.woff2`），导致：
- **本地 dev**：Vite 按 base 路径提供 public 资源，`/projects/...` 请求 404
- **线上 build**：HTML 中 JS/CSS 路径由 Vite 自动加前缀，但 JS 内部的图片/字体 URL 不会自动处理，仍然 404

#### 根因分析

1. **Vite base 的局限性**：`base` 只自动处理 HTML 中的 `<script>`/`<link>` 标签和 `import` 的资源，**不处理 JS 运行时拼接的字符串 URL**
2. **CSS 中的 `url()` 不感知 base**：`@font-face` 中的 `url('/fonts/...')` 在 build 后不会自动加前缀
3. **`<img src>` 是运行时设置的**：React 中 `src={url}` 是 JS 运行时赋值，Vite 无法在构建时改写

#### 最终解决方案

1. **`vite.config.ts` base 统一为 `/`**：因为 GitHub Pages 配置了自定义域名 `xiaoka.pojuai.com`，资源路径为根路径
```typescript
export default defineConfig({
  base: '/',
  // ...
});
```

> **注意**：如果 GitHub Pages **没有**配置自定义域名，base 应为条件式：
> `base: command === 'build' ? '/仓库名/' : '/'`

2. **创建 `publicAsset()` 工具函数**：统一为 public 资源拼接 base 前缀
```typescript
export function publicAsset(path: string): string {
  if (/^(data:|blob:|https?:|\/\/)/.test(path)) return path; // data: URL 不处理
  const cleanPath = path.replace(/^\//, '');
  return `${import.meta.env.BASE_URL}${cleanPath}`;
}
```

3. **所有 public 资源引用走 `publicAsset()`**：
   - `useProjectImage` Hook 中的图片探测 URL
   - `buildCardSVG` 中 SVG `<image href>` 的 avatar 路径
   - `ServicesSection` 中 `sampleImage` 的 `<img src>`
   - `CardEditor` / `SettingsPanel` 中 avatar 背景图 `url()`

4. **`@font-face` 从 CSS 移至 JS 动态注入**：CSS 中的 `url()` 无法感知 base，改为在 `main.tsx` 中用 `import.meta.env.BASE_URL` 拼接后注入 `<style>`

5. **`BrowserRouter` 添加 `basename`**：线上部署在子路径下时，路由需要感知 basename
```tsx
<BrowserRouter basename={import.meta.env.BASE_URL}>
```

6. **`404.html` SPA shim 的 `pathSegmentsToKeep`**：
   - 使用自定义域名时设为 `0`（路径不含仓库名）
   - 使用 `xxx.github.io/仓库名/` 时设为 `1`

7. **`index.html` 移除硬编码 preload**：`<link rel="preload" href="/fonts/...">` 不感知 base，移除后由 JS 动态注入

#### 关键经验

| 序号 | 经验教训 | 避免方法 |
|------|---------|---------|
| 9 | Vite base 不处理 JS 运行时 URL | 用 `import.meta.env.BASE_URL` 手动拼接 |
| 10 | CSS `url()` 不感知 Vite base | `@font-face` 移至 JS 动态注入 |
| 11 | 自定义域名下 base 应为 `/` | 有自定义域名用 `/`，无则用 `/仓库名/` |
| 12 | `BrowserRouter` 需要感知子路径 | 传 `basename={import.meta.env.BASE_URL}` |
| 13 | 所有 public 资源引用集中管理 | 统一走 `publicAsset()` 工具函数 |
| 14 | `404.html` shim 需匹配域名模式 | 自定义域名 `pathSegmentsToKeep=0`，子路径 `=1` |

#### 标准流程（部署到 GitHub Pages）

```
1. 确认是否使用自定义域名
   ├── 有自定义域名 → base='/'
   └── 无自定义域名 → base=条件式（dev=/, build=/仓库名/）

2. 创建 publicAsset() 工具函数
3. 所有 public 资源引用走 publicAsset()
4. @font-face 移至 JS 动态注入
5. BrowserRouter 添加 basename
6. 404.html pathSegmentsToKeep 匹配域名模式
7. index.html 移除硬编码资源路径
8. 本地 dev 验证 + build 产物验证 + 线上验证
```

---

### 案例6：3D 工牌头像在 data URL SVG 中不显示

#### 问题描述
3D 工牌卡片使用 `buildCardSVG` 生成 SVG 字符串，编码为 `data:image/svg+xml` URL 后通过 `new Image()` 加载为 Three.js 纹理。SVG 中通过 `<image href="/avatar-default.jpg">` 引用默认头像，但头像始终不显示（只显示占位圆形和姓名）。

#### 根因分析

**浏览器安全限制**：当 SVG 以 `data:` URL 方式加载（如 `img.src = 'data:image/svg+xml,...'`）时，浏览器会将其视为独立文档，**不会加载 SVG 内部通过 `<image href>` 引用的外部资源**。这是为了防止 data URL 上下文中的信息泄露。

- 用户上传的头像不受影响，因为上传时已转为 base64 data URL
- 默认头像 `/avatar-default.jpg` 是文件路径引用，在 data URL SVG 中无法加载

#### 最终解决方案

在 `Lanyard.tsx` 生成 SVG 前，先把 avatar 图片 `fetch` 转为 data URL，再嵌入 SVG：

```typescript
const avatar = cardConfig.avatar;
if (avatar && !avatar.startsWith('data:')) {
  // avatar 是文件路径，需要转为 data URL
  fetch(publicAsset(avatar))
    .then(res => res.blob())
    .then(blob => {
      const reader = new FileReader();
      reader.onload = () => buildAndLoad(reader.result); // data URL
      reader.readAsDataURL(blob);
    });
} else {
  buildAndLoad(avatar); // 已是 data URL 或为空
}
```

#### 关键经验

| 序号 | 经验教训 | 避免方法 |
|------|---------|---------|
| 15 | data URL SVG 不能引用外部资源 | 先将图片转为 data URL 嵌入 SVG |
| 16 | Three.js 纹理加载 SVG 等同于 data URL | 同上 |
| 17 | 用户上传的 base64 不受此限制 | 上传时已转 data URL，可直接用 |
