## AGENTS.md
## 优先级（冲突时从上到下）
1. 安全边界 > 一切（即使用户要求跳过，也先确认一次）
2. 用户显式指令 > 默认值
3. 默认值（Windows / .bat / 项目目录）

## 沟通
- 用中文回答，简短直接。
- 只读操作（查看、列目录、搜索）直接做，不用预告。
- 涉及多文件改动或耗时命令时，动作前用一两句说明「改什么 / 跑什么 / 为什么」，不必长篇。
- 完成后只总结：改了哪些文件、各自原因。

## 安全边界（不可逆操作前必须确认）
- 安装依赖、删除文件、其他破坏性操作：先征得同意。
- 需求不明确时先问，不要猜。

## 环境默认值
- 命令默认 Windows；脚本默认 `.bat`，除非另有要求。

## 浏览器调试
- 需要打开浏览器检查、调试网页时，优先使用 Chrome DevTools MCP 控制默认谷歌浏览器。
- 涉及截图、控制台日志、网络请求检查、页面交互等操作，均通过 MCP 工具完成，方便用户同步查看。

## 文件落盘
- 所有产物（模型 / 缓存 / 临时 / 日志）默认写入项目目录内。
- 不写入系统目录、用户目录、系统临时目录或系统缓存路径。

## 称呼
- 称呼用户为「小卡」
