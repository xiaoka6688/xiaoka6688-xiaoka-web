# 自定义管理后台 - 项目计划文档

## 📅 创建日期
2026-07-28

## 🎯 项目概述

### 背景
当前网站的项目板块和服务板块采用高度模板化的数据结构，每次新增或修改内容都需要：
1. 修改 TypeScript 数据文件
2. 截图并放到正确的目录
3. 重新构建和部署

这导致内容更新效率低下，非技术人员无法自主管理。

### 目标
构建一个**自定义管理后台**，让用户可以通过可视化界面管理项目和服务内容，实现：
- 实时预览修改效果
- 无需代码即可增删改查内容
- 图片上传和管理
- 数据可持久化存储

### 核心价值
- ✅ 内容更新效率提升 10 倍以上
- ✅ 非技术人员可自主管理
- ✅ 实时预览，所见即所得
- ✅ 为后续云端迁移奠定基础

---

## 📐 技术方案

### 三阶段演进路线

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   方案 A    │ ──▶ │   方案 B    │ ──▶ │   方案 C    │
│  纯前端     │     │  + 云存储   │     │  完整 CMS   │
│ localStorage│     │   BaaS     │     │   系统      │
└─────────────┘     └─────────────┘     └─────────────┘
     ▲                    ▲                    ▲
     │                    │                    │
  1-2 天 MVP          3-5 天增强           1-2 周完整
```

---

## 📋 方案 A：纯前端 + localStorage（起步阶段）

### 1. 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户界面                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Settings Panel（设置栏）                             │    │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────────────┐   │    │
│  │ │功能设置 │ │工牌编辑 │ │ 自定义管理（新增）  │   │    │
│  │ └─────────┘ └─────────┘ └─────────────────────┘   │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     AdminPanel 组件                          │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Tab 切换: [项目管理] [服务管理] [数据导入导出]      │    │
│  └─────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 项目列表                                             │    │
│  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────────┐     │    │
│  │ │项目1│ │项目2│ │项目3│ │项目4│ │ + 新增  │     │    │
│  │ └─────┘ └─────┘ └─────┘ └─────┘ └─────────┘     │    │
│  └─────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 编辑表单（点击项目后展开）                           │    │
│  │  基础信息                                            │    │
│  │  ├─ 名称: [________________]                        │    │
│  │  ├─ Slug: [________________] (自动生成/可修改)      │    │
│  │  ├─ 简介: [________________]                        │    │
│  │  └─ 描述: [________________________]               │    │
│  │                                                      │    │
│  │  视觉资源                                            │    │
│  │  ├─ 主图: [选择文件] [预览缩略图]                   │    │
│  │  └─ 功能截图: [+] [图片1] [图片2] [图片3]          │    │
│  │                                                      │    │
│  │  元数据                                              │    │
│  │  ├─ 标签: [tag1] [tag2] [+]                         │    │
│  │  ├─ 技术栈: [tech1] [tech2] [+]                     │    │
│  │  └─ 链接: GitHub [________] 在线 [________]         │    │
│  │                                                      │    │
│  │  [预览] [保存] [删除]                                │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       数据存储层                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ localStorage                                         │    │
│  │                                                      │    │
│  │  xiaoka-admin-projects-v1                            │    │
│  │  └─ ProjectTemplate[] (自定义项目数据)               │    │
│  │                                                      │    │
│  │  xiaoka-admin-services-v1                            │    │
│  │  └─ ServiceTemplate[] (自定义服务数据)               │    │
│  │                                                      │    │
│  │  xiaoka-admin-images-v1                              │    │
│  │  └─ { [key]: base64String } (图片数据)              │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        渲染层                                │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ ProjectsSection / ServicesSection                    │    │
│  │                                                      │    │
│  │ 数据读取逻辑:                                        │    │
│  │ const projects = customProjects.length > 0           │    │
│  │   ? customProjects                                   │    │
│  │   : defaultProjects;                                 │    │
│  │                                                      │    │
│  │ 渲染逻辑不变，只是数据源可切换                       │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 2. 数据结构设计

```typescript
// 项目模板（简化版，单语言）
interface ProjectTemplate {
  id: string;              // 唯一ID (uuid)
  slug: string;            // URL标识 (自动生成或手动)
  name: string;            // 项目名称
  tagline: string;         // 一句话简介
  description: string;     // 详细描述
  
  heroImage: string;       // 主图 (base64 或 URL)
  features: {
    id: string;
    title: string;
    description: string;
    image: string;         // base64 或 URL
  }[];
  
  tags: string[];          // 标签
  techStack: string[];     // 技术栈
  
  githubUrl?: string;      // GitHub链接
  liveUrl?: string;        // 在线地址
  
  order: number;           // 显示顺序
  visible: boolean;        // 是否显示
  createdAt: number;       // 创建时间戳
  updatedAt: number;       // 更新时间戳
}

// 服务模板（简化版，单语言）
interface ServiceTemplate {
  id: string;
  slug: string;
  name: string;
  subtitle: string;
  tagline: string;
  features: string[];      // 3条简短描述
  tags: string[];
  visitUrl?: string;
  order: number;
  visible: boolean;
  createdAt: number;
  updatedAt: number;
}

// 图片存储
interface ImageStore {
  [key: string]: string;   // key: "project-{id}-hero" / "project-{id}-feature-{n}"
                           // value: base64 data URL
}
```

### 3. 实现计划

#### 阶段 1：基础框架（1天）
- [ ] 创建 `AdminPanel` 组件骨架
- [ ] 在 Settings Panel 中添加"自定义管理"入口
- [ ] 设计 Tab 切换 UI（项目管理/服务管理/导入导出）

#### 阶段 2：项目管理（1天）
- [ ] 项目列表展示（从 localStorage 读取）
- [ ] 新增项目表单
- [ ] 编辑项目表单
- [ ] 删除项目确认
- [ ] 项目排序（拖拽或上下移动）

#### 阶段 3：图片上传（0.5天）
- [ ] 图片选择器（input type="file"）
- [ ] 图片压缩（限制最大尺寸）
- [ ] 转 base64 存入 localStorage
- [ ] 图片预览和删除

#### 阶段 4：服务管理（0.5天）
- [ ] 服务列表展示
- [ ] 新增/编辑/删除服务
- [ ] 服务排序

#### 阶段 5：数据集成（0.5天）
- [ ] 修改 ProjectsSection 读取自定义数据
- [ ] 修改 ServicesSection 读取自定义数据
- [ ] 数据优先级：localStorage > 默认静态数据

#### 阶段 6：导入导出（0.5天）
- [ ] 导出为 JSON 文件
- [ ] 从 JSON 文件导入
- [ ] 数据合并策略

### 4. 图片存储说明

**方案 A 的图片存储方式：**

```
用户上传图片
      │
      ▼
┌─────────────────┐
│ FileReader API  │
│ 读取为 base64   │
└─────────────────┘
      │
      ▼
┌─────────────────┐
│ 图片压缩        │
│ (canvas resize) │
│ 最大 800px 宽   │
└─────────────────┘
      │
      ▼
┌─────────────────┐
│ 存入 localStorage│
│ key: 图片ID     │
│ value: base64   │
└─────────────────┘
```

**存储容量限制：**
- localStorage 通常限制 5-10MB
- 压缩后的图片约 50-200KB/张
- 预计可存储 25-100 张图片
- 超出限制时提示用户导出备份

**⚠️ 方案 A 的局限性：**
- 图片数据存在浏览器本地，换设备会丢失
- 存储容量有限
- 无法多人协作

---

## 📋 方案 B：前端 + 云存储（增强阶段）

### 1. 技术架构升级

```
┌─────────────────────────────────────────────────────────────┐
│                        前端（不变）                          │
│  AdminPanel → 数据读写 API                                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据访问层（新增）                         │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ DataService                                          │    │
│  │  ├─ getProjects(): ProjectTemplate[]                │    │
│  │  ├─ saveProject(project): void                      │    │
│  │  ├─ deleteProject(id): void                         │    │
│  │  ├─ uploadImage(file): string (返回URL)             │    │
│  │  └─ syncData(): void                                │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     云服务（BaaS）                           │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │   数据库        │  │   对象存储      │                  │
│  │  (Firestore/    │  │  (S3/R2/OSS)   │                  │
│  │   Supabase)     │  │                 │                  │
│  └─────────────────┘  └─────────────────┘                  │
│         │                     │                             │
│         └──────────┬──────────┘                             │
│                    ▼                                        │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 存储内容                                             │    │
│  │  ├─ projects/ (JSON文档)                            │    │
│  │  ├─ services/ (JSON文档)                            │    │
│  │  └─ images/   (图片文件)                            │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 2. 推荐的 BaaS 方案

| 方案 | 数据库 | 存储 | 免费额度 | 推荐度 |
|------|--------|------|----------|--------|
| **Firebase** | Firestore | Storage | 1GB存储, 10GB传输/月 | ⭐⭐⭐⭐⭐ |
| **Supabase** | PostgreSQL | Storage | 500MB数据库, 1GB存储 | ⭐⭐⭐⭐ |
| **LeanCloud** | 内置 | 内置 | 国内访问快 | ⭐⭐⭐⭐ |
| **Cloudflare** | D1/R2 | R2 | 10GB存储免费 | ⭐⭐⭐ |

**推荐 Firebase 的原因：**
- 无需服务器，纯前端 SDK
- 实时同步（多设备自动同步）
- 图片直接上传到 Storage
- 免费额度足够个人使用
- 社区成熟，文档完善

### 3. 数据迁移路径

```
方案 A (localStorage)
        │
        ▼
┌─────────────────────────────────────────┐
│  迁移脚本                                │
│  1. 读取 localStorage 数据              │
│  2. 上传图片到云存储，获取 URL          │
│  3. 替换 base64 为 URL                  │
│  4. 写入云端数据库                       │
└─────────────────────────────────────────┘
        │
        ▼
方案 B (云存储)

用户操作：
1. 在 AdminPanel 点击"迁移到云端"
2. 输入 Firebase 配置信息
3. 自动执行迁移
4. 完成后切换数据源为云端
```

### 4. 实现计划

#### 阶段 1：接入 Firebase（1天）
- [ ] 创建 Firebase 项目
- [ ] 配置 Firebase SDK
- [ ] 实现匿名认证（可选，保护数据）

#### 阶段 2：数据层重构（1天）
- [ ] 创建 DataService 抽象层
- [ ] 实现 Firebase CRUD 操作
- [ ] 保持 localStorage 作为 fallback

#### 阶段 3：图片云存储（1天）
- [ ] 实现图片上传到 Firebase Storage
- [ ] 获取图片公开 URL
- [ ] 图片压缩和优化

#### 阶段 4：数据同步（0.5天）
- [ ] 实现离线支持（localStorage 缓存）
- [ ] 网络恢复时自动同步
- [ ] 冲突检测和解决

#### 阶段 5：迁移工具（0.5天）
- [ ] 从 localStorage 导出
- [ ] 导入到 Firebase
- [ ] 验证数据完整性

---

## 📋 方案 C：完整 CMS 系统（完整阶段）

### 1. 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                      管理后台前端                            │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ React Admin / Strapi Admin / 自建                   │    │
│  │  ├─ 可视化编辑器                                    │    │
│  │  ├─ 媒体库管理                                      │    │
│  │  ├─ 内容审核                                        │    │
│  │  └─ 版本历史                                        │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      后端服务                                │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Node.js / Strapi / Directus                         │    │
│  │  ├─ RESTful API / GraphQL                           │    │
│  │  ├─ 认证授权                                        │    │
│  │  ├─ 文件上传处理                                    │    │
│  │  └─ Webhook (触发构建)                              │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据层                                  │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │   PostgreSQL    │  │   S3 / OSS      │                  │
│  │   (结构化数据)  │  │   (媒体文件)    │                  │
│  └─────────────────┘  └─────────────────┘                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      前端网站                                │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ xiaoka-web (当前项目)                               │    │
│  │  ├─ 构建时从 CMS 拉取数据                           │    │
│  │  ├─ 或运行时通过 API 获取数据                       │    │
│  │  └─ 图片从 CDN 加载                                 │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 2. 推荐的 CMS 方案

| 方案 | 类型 | 特点 | 适合场景 |
|------|------|------|----------|
| **Strapi** | 自托管 | 开源，功能完整 | 需要完全控制 |
| **Directus** | 自托管 | 轻量，API优先 | 快速上手 |
| **Sanity** | 云服务 | 实时协作，GROQ查询 | 团队协作 |
| **Contentful** | 云服务 | 企业级，稳定 | 商业项目 |

**推荐 Strapi 的原因：**
- 开源免费，可自托管
- 内置管理后台 UI
- 自动生成 REST/GraphQL API
- 媒体库管理完善
- 插件生态丰富

### 3. 实现计划（如需要）

#### 阶段 1：部署 Strapi（0.5天）
- [ ] 部署到 Railway / Vercel / 自有服务器
- [ ] 配置数据库（PostgreSQL）
- [ ] 配置媒体存储（S3 / Cloudflare R2）

#### 阶段 2：定义内容模型（0.5天）
- [ ] Project 内容类型
- [ ] Service 内容类型
- [ ] Feature 组件（重复字段）
- [ ] 媒体关联

#### 阶段 3：前端对接（1天）
- [ ] 替换静态数据为 API 调用
- [ ] 实现增量静态生成（ISR）
- [ ] 图片使用 CDN URL

#### 阶段 4：高级功能（2-3天）
- [ ] 版本历史和回滚
- [ ] 内容审核工作流
- [ ] 多语言支持
- [ ] SEO 自动生成

---

## ❓ 关键问题解答

### Q1：方案A → B → C 是否可以平滑过渡？

**✅ 是的，完全支持平滑过渡**

```
方案 A                           方案 B                           方案 C
   │                                │                                │
   ▼                                ▼                                ▼
┌─────────────┐              ┌─────────────┐              ┌─────────────┐
│ localStorage │    迁移脚本  │  Firebase   │    API对接   │   Strapi    │
│   数据层     │ ──────────▶ │   数据层    │ ──────────▶ │   数据层    │
└─────────────┘              └─────────────┘              └─────────────┘
   │                                │                                │
   ▼                                ▼                                ▼
┌─────────────┐              ┌─────────────┐              ┌─────────────┐
│  前端渲染    │    不变      │  前端渲染    │    不变      │  前端渲染    │
│   逻辑      │ ──────────▶ │   逻辑      │ ──────────▶ │   逻辑      │
└─────────────┘              └─────────────┘              └─────────────┘
```

**关键设计：数据访问层抽象**

```typescript
// 统一的数据接口
interface DataProvider {
  getProjects(): Promise<ProjectTemplate[]>;
  saveProject(project: ProjectTemplate): Promise<void>;
  deleteProject(id: string): Promise<void>;
  uploadImage(file: File): Promise<string>;
  exportData(): Promise<string>;
  importData(data: string): Promise<void>;
}

// 方案 A 实现
class LocalStorageProvider implements DataProvider { ... }

// 方案 B 实现
class FirebaseProvider implements DataProvider { ... }

// 方案 C 实现
class StrapiProvider implements DataProvider { ... }
```

通过接口抽象，切换方案只需更换 Provider 实现，前端代码无需修改。

---

### Q2：图片存储的演进路径

**当前状态（本地调试）：**
```
图片文件 → public/projects/xxx/hero.png → 本地服务器访问
```

**方案 A：**
```
用户上传 → FileReader → base64 → localStorage
                                        │
                                        ▼
                          前端直接读取 base64 渲染
```

**方案 B：**
```
用户上传 → 压缩 → Firebase Storage → 获取公开URL
                                          │
                                          ▼
                            localStorage 存储 URL（非图片数据）
                                          │
                                          ▼
                            前端通过 URL 加载图片
```

**方案 C：**
```
用户上传 → Strapi → S3/R2/OSS → CDN分发
                                      │
                                      ▼
                        前端通过 CDN URL 加载图片
```

**⚠️ 重要说明：**
- 方案 A 的 base64 图片**不会**自动迁移到云端
- 迁移到方案 B 时，需要：
  1. 从 localStorage 读取 base64 图片
  2. 上传到云存储
  3. 获取 URL 替换 base64
  4. 清理 localStorage 中的图片数据
- 迁移工具会自动处理这个过程

---

## 📊 工作量估算

| 阶段 | 功能 | 工作量 | 优先级 |
|------|------|--------|--------|
| **方案 A** | | | |
| A1 | AdminPanel 基础框架 | 1 天 | P0 |
| A2 | 项目管理 CRUD | 1 天 | P0 |
| A3 | 图片上传 | 0.5 天 | P0 |
| A4 | 服务管理 | 0.5 天 | P1 |
| A5 | 数据集成 | 0.5 天 | P0 |
| A6 | 导入导出 | 0.5 天 | P1 |
| | **小计** | **4 天** | |
| **方案 B** | | | |
| B1 | Firebase 接入 | 1 天 | P2 |
| B2 | 数据层重构 | 1 天 | P2 |
| B3 | 云存储 | 1 天 | P2 |
| B4 | 数据同步 | 0.5 天 | P2 |
| B5 | 迁移工具 | 0.5 天 | P2 |
| | **小计** | **4 天** | |
| **方案 C** | | | |
| C1 | Strapi 部署 | 0.5 天 | P3 |
| C2 | 内容模型 | 0.5 天 | P3 |
| C3 | 前端对接 | 1 天 | P3 |
| C4 | 高级功能 | 2-3 天 | P3 |
| | **小计** | **4-5 天** | |

**总计：12-13 天（完整实现）**

---

## 🎯 里程碑

### 里程碑 1：MVP（方案 A 完成）
- ✅ 可通过 UI 管理项目和服务
- ✅ 支持图片上传
- ✅ 实时预览
- ✅ 数据导入导出
- **预计完成：4 天后**

### 里程碑 2：云端同步（方案 B 完成）
- ✅ 数据云端存储
- ✅ 多设备同步
- ✅ 图片云存储
- **预计完成：8 天后**

### 里程碑 3：企业级（方案 C 完成）
- ✅ 完整 CMS 功能
- ✅ 版本控制
- ✅ 多语言支持
- **预计完成：13 天后**

---

## 📝 风险和注意事项

### 技术风险
1. **localStorage 容量限制** - 需要图片压缩，监控存储使用量
2. **图片 base64 膨胀** - base64 比原始文件大约 33%，需要压缩
3. **数据丢失风险** - 需要定期导出备份提醒

### 用户体验风险
1. **表单复杂度** - 需要分步引导，避免一次性展示太多字段
2. **图片上传体验** - 需要进度条、预览、错误处理
3. **移动端适配** - AdminPanel 需要响应式设计

### 安全风险
1. **XSS 防护** - 用户输入需要转义
2. **图片上传限制** - 限制文件类型和大小
3. **数据验证** - 前后端都需要验证

---

## 📚 参考资源

- [Firebase Documentation](https://firebase.google.com/docs)
- [Strapi Documentation](https://docs.strapi.io/)
- [localStorage API](https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage)
- [FileReader API](https://developer.mozilla.org/en-US/docs/Web/API/FileReader)

---

## ✏️ 版本历史

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-07-28 | v1.0 | 初始计划文档 |

---

*文档创建：2026-07-28*
*最后更新：2026-07-28*
